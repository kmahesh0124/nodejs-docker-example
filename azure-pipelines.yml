# Node.js with React
# Build a Node.js project that uses React.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

variables:
  imageName: latest:$(Build.SourceBranchName)-$(Build.BuildNumber)

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    echo "SECRET_AUTH_AZURE_CLIENT_ID=$(SECRET_AUTH_AZURE_CLIENT_ID)" > .env
    echo "Build.BuildNumber: $(Build.BuildNumber)"
    echo "Build.SourceBranchName: $(Build.SourceBranchName)"
    echo "imageName: $(imageName)"
#    docker-compose build
  displayName: 'create .env file'
- script: |
    echo "SECRET_AUTH_AZURE_CLIENT_ID=$(SECRET_AUTH_AZURE_CLIENT_ID)" > .env
    echo "Build.BuildNumber: $(Build.BuildNumber)"
    echo "Build.SourceBranchName: $(Build.SourceBranchName)"
    echo "imageName: $(imageName)"
#    docker-compose build
  displayName: 'create .env file'


#- script: |
#    docker-compose run app npm i
#  displayName: 'Install node modules'

- script: |
    def dockerPswd='Arya1010'
    docker build -t agiletestpk.azurecr.io/$(imageName) .
    docker login agiletestpk.azurecr.io --u Agiletestpk -p $(dockerPswd) $(dockerId).azurecr.io
    docker push $(dockerId).azurecr.io/$(imageName)
  displayName: 'Build Docker Image'


